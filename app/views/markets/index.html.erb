<h1>Markets</h1>

<section class="markets">
  <% @facade.markets.each do |market| %>
    <div id="market">
      <li><%= market.name %> - <%= market.city %>, <%= market.state %> <%= button_to "More Info", "/markets/#{market.id}", method: :get %></li>
    </div>
  <% end %>
</section>